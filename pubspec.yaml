name: timesnewsroman
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  url_launcher: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^1.0.0
flutter:
  fonts:
    - family: brushtip
      fonts:
        - asset: 'fonts/brushtip.ttf'

  uses-material-design: true
